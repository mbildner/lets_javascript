import { Person } from '../../src/js/Person.js'

describe('Person', () => {
  describe('render', () => {
    it('displays the provided title', () => {
      expect(Person).not.toBeUndefined()
    })
  })
})
